<%= form_for(@sns_push_key) do |f| %>
  <% if @sns_push_key.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sns_push_key.errors.count, "error") %> prohibited this sns_push_key from being saved:</h2>

      <ul>
      <% @sns_push_key.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
