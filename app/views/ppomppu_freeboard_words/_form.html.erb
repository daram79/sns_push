<%= form_for(@ppomppu_freeboard_word) do |f| %>
  <% if @ppomppu_freeboard_word.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ppomppu_freeboard_word.errors.count, "error") %> prohibited this ppomppu_freeboard_word from being saved:</h2>

      <ul>
      <% @ppomppu_freeboard_word.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
