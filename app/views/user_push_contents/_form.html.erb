<%= form_for(@user_push_content) do |f| %>
  <% if @user_push_content.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_push_content.errors.count, "error") %> prohibited this user_push_content from being saved:</h2>

      <ul>
      <% @user_push_content.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
