<%= form_for(@sn) do |f| %>
  <% if @sn.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sn.errors.count, "error") %> prohibited this sn from being saved:</h2>

      <ul>
      <% @sn.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
